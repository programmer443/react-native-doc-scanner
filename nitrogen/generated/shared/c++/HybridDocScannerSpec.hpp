///
/// HybridDocScannerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NativeFrameResult` to properly resolve imports.
namespace margelo::nitro::docscanner { struct NativeFrameResult; }
// Forward declaration of `HybridFrameSpec` to properly resolve imports.
namespace margelo::nitro::camera { class HybridFrameSpec; }
// Forward declaration of `NativeFaceFrameResult` to properly resolve imports.
namespace margelo::nitro::docscanner { struct NativeFaceFrameResult; }
// Forward declaration of `LoadModelsResult` to properly resolve imports.
namespace margelo::nitro::docscanner { struct LoadModelsResult; }
// Forward declaration of `ModelPaths` to properly resolve imports.
namespace margelo::nitro::docscanner { struct ModelPaths; }
// Forward declaration of `RawOcrResultNative` to properly resolve imports.
namespace margelo::nitro::docscanner { struct RawOcrResultNative; }
// Forward declaration of `Quad` to properly resolve imports.
namespace margelo::nitro::docscanner { struct Quad; }

#include "NativeFrameResult.hpp"
#include <memory>
#include <VisionCamera/HybridFrameSpec.hpp>
#include "NativeFaceFrameResult.hpp"
#include "LoadModelsResult.hpp"
#include <NitroModules/Promise.hpp>
#include "ModelPaths.hpp"
#include "RawOcrResultNative.hpp"
#include <string>
#include "Quad.hpp"
#include <optional>

namespace margelo::nitro::docscanner {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `DocScanner`
   * Inherit this class to create instances of `HybridDocScannerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridDocScanner: public HybridDocScannerSpec {
   * public:
   *   HybridDocScanner(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridDocScannerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridDocScannerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridDocScannerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual NativeFrameResult analyzeFrame(const std::shared_ptr<margelo::nitro::camera::HybridFrameSpec>& frame) = 0;
      virtual NativeFaceFrameResult analyzeFaceFrame(const std::shared_ptr<margelo::nitro::camera::HybridFrameSpec>& frame) = 0;
      virtual std::shared_ptr<Promise<LoadModelsResult>> loadModels(const ModelPaths& config) = 0;
      virtual std::shared_ptr<Promise<RawOcrResultNative>> captureAndExtract(const std::string& photoPath, const std::string& documentType, const std::optional<Quad>& quad) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "DocScanner";
  };

} // namespace margelo::nitro::docscanner
